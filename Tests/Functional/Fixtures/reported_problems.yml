services:
    pdo:
        class: PDO
        arguments:
            dsn: "mysql:dbname=dbname;host=host"
            user: "user"
            password: "password"
        calls:
            - [setAttribute, [3, 2]]
